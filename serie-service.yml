server:
  port: 8098

spring:
  data:
    mongodb: 
      uri: mongodb://localhost:27017/serieBack

eureka:
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

  
##ESTO SE DESCOMENTA DESPUES
  ##rabbitmq:
    ##username: guest
    ##password: guest
    ##host: localhost
    ##port: 5672

##queue:
  ##movie:
    ##name: MovieQueue

##ESTO NO SE PARA QUE ES
##management:
  ##endpoints:
    ##web:
      ##exposure:
        ##include: info
  ##info:
    ##env:
      ##enabled: true
  ##endpoint:
    ##info:
      ##enabled: true

info:
  app:
    name: ${spring.application.name}
    version: 1.0.0
    description: Movie Microservice
  svc:
    intanceid: ${eureka.instance.instance-id}
    port: ${server.port}